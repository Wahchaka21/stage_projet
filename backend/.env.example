## Copiez ce fichier en `.env` et remplissez les valeurs

# Clé secrète utilisée pour signer/vérifier les JWT (HS256)
# Générez une valeur forte (ex: `openssl rand -base64 64`)
JWT_SECRET=change_me_with_a_strong_random_secret

# Contexte JWT (facultatif mais recommandé)
JWT_ISSUER=your-app
JWT_AUDIENCE=your-api

# Base de données
MONGO_URL=mongodb://localhost:27017/projet_stage

# Port HTTP
PORT=3000

# CORS: liste des origines autorisées (séparées par des virgules)
# Exemple pour React dev server et un autre front
CORS_ORIGINS=http://localhost:5173,http://localhost:3001
